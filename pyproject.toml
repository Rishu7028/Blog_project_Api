[project]
name = "blog-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rishu7028", email = "rishu.2201180ec@iiitbh.ac.in" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "pymongo>=4.18.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
blog-project = "blog_project:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
